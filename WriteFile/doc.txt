Флаги «r» и «w» в Node.js указывают на поведение открываемого файла в методах работы с файловой системой (например, fs.open() из модуля fs). 
Эти флаги могут быть частью параметра flags в объекте options.